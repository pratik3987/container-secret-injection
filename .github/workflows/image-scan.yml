name: Image Security Scan

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  trivy-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build images
        run: |
          docker build -f Dockerfile.webhook -t example/vault-webhook:latest .
          docker build -f Dockerfile.runner -t example/vault-runner:latest .
          docker build -f cmd/controller/Dockerfile -t example/vault-controller:latest .
      - name: Scan images with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: |
            example/vault-webhook:latest
            example/vault-runner:latest
            example/vault-controller:latest
          format: 'table'
          exit-code: '1'
          severity: 'CRITICAL,HIGH'
