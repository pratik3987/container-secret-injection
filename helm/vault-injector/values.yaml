replicaCount: 1

image:
  controller:
    # Replace <OWNER> with your GitHub org/user
    repository: ghcr.io/<OWNER>/vault-webhook
    tag: controller
    pullPolicy: IfNotPresent
  webhook:
    repository: ghcr.io/<OWNER>/vault-webhook
    tag: webhook
    pullPolicy: IfNotPresent
  runner:
    repository: ghcr.io/<OWNER>/vault-webhook
    tag: runner
    pullPolicy: IfNotPresent

serviceAccount:
  create: true
  name: vault-webhook

rBAC:
  create: true

webhook:
  serviceName: vault-webhook
  serviceNamespace: default
  tlsSecretName: vault-webhook-tls

controller:
  enabled: true
  image: "{{ .Values.image.controller.repository }}:{{ .Values.image.controller.tag }}"
  replicas: 1

webhookDeployment:
  enabled: true
  image: "{{ .Values.image.webhook.repository }}:{{ .Values.image.webhook.tag }}"
  replicas: 1

resources: {}
